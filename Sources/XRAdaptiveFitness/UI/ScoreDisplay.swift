// ScoreDisplay.swift — Owner: Tri Nguyen
import SwiftUI

struct ScoreDisplay: View {
    var body: some View {
        // TODO: Tri implements — score + multiplier display (top right, fades at low speed)
        EmptyView()
    }
}
