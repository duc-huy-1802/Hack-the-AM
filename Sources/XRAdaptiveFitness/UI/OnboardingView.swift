// OnboardingView.swift — Owner: Tri Nguyen
import SwiftUI

struct OnboardingView: View {
    var body: some View {
        // TODO: Tri implements — initial pairing + setup screen
        EmptyView()
    }
}
